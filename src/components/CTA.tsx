const CTA = () => {
  return (
    <section className="cta">
      <div className="cta-inner">
        <span className="cta-badge">Get started today</span>

        <h2>Turn WhatsApp chats into sales</h2>

        <p>
          Use AI to reply faster, qualify leads automatically, and close more
          deals — without hiring more people.
        </p>

        <div className="cta-actions">
          <button className="cta-primary">Start Now</button>
          <button className="cta-secondary">Book a Demo</button>
        </div>
      </div>
    </section>
  );
};

export default CTA;
